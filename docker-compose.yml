version: "3.9"

services:
  credit-risk-platform:
    build: .
    container_name: credit_risk_platform
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    restart: unless-stopped
